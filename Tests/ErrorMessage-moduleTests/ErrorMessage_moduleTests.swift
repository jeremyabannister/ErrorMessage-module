import XCTest
@testable import ErrorMessage_module

final class ErrorMessage_moduleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ErrorMessage_module().text, "Hello, World!")
    }
}
